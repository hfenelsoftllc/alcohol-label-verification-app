# Backend runtime dependencies — pinned for reproducible builds (FedRAMP CM-2).
# Update deliberately; CI runs pip-audit against this file on every PR.
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
python-multipart==0.0.32   # multipart/form-data parsing for batch upload
anthropic==0.109.1         # Claude Vision API client
pytesseract==0.3.13        # local Tesseract OCR fallback (needs tesseract-ocr binary)
pillow==12.2.0             # image loading for Tesseract
opencv-python-headless==4.12.0.88  # image quality assessment (no GUI deps)
numpy==2.2.6                       # array ops for OpenCV
